# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
GET     /health                           controllers.HealthController.ok()

POST    /payments                         controllers.PaymentsController.createPayment()

GET     /payments/:id                     controllers.PaymentsController.getPayment(id: String)
GET     /payments                         controllers.PaymentsController.getPayments()
